# WedO — Wedding Photography & Sharing (MVP scaffold)

This folder contains a production-ready MVP scaffold for the WedO app built with Expo, React Native and Firebase (TypeScript).

Follow the README and fill environment variables in `.env` before running.
